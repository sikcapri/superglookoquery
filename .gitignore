node_modules
npm-debug.log
.env
*.local
data/
*.mcpb
# store.js's persist() writes here before an atomic rename; a crash between
# the write and the rename can orphan one (hit for real while regenerating
# examples/podquery.db during development — see docs/TODO.md).
*.tmp-*